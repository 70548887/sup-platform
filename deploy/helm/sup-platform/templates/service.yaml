apiVersion: v1
kind: Service
metadata:
  name: {{ include "sup-platform.fullname" . }}
  labels:
    {{- include "sup-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sup-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
